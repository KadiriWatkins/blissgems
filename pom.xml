<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.xoperr</groupId>
    <artifactId>BlissGems</artifactId>
    <version>1.0</version>
    <packaging>jar</packaging>

    <name>BlissGems</name>
    <description>Bliss SMP Gem System</description>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <!-- Paper/Spigot API -->
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <!-- PlaceholderAPI -->
        <repository>
            <id>placeholderapi</id>
            <url>https://repo.extendedclip.com/content/repositories/placeholderapi/</url>
        </repository>
        <!-- FastStats -->
        <repository>
            <id>thenextlvl-releases</id>
            <name>TheNextLvl</name>
            <url>https://repo.thenextlvl.net/releases</url>
        </repository>
        <!-- WorldGuard -->
        <repository>
            <id>enginehub-maven</id>
            <url>https://maven.enginehub.org/repo/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Paper API (includes Bukkit/Spigot) -->
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>1.21-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>

        <!-- PlaceholderAPI -->
        <dependency>
            <groupId>me.clip</groupId>
            <artifactId>placeholderapi</artifactId>
            <version>2.11.6</version>
            <scope>provided</scope>
        </dependency>

        <!-- FastStats Metrics -->
        <dependency>
            <groupId>dev.faststats.metrics</groupId>
            <artifactId>bukkit</artifactId>
            <version>0.17.2</version>
        </dependency>

        <!-- WorldGuard (soft dependency) -->
        <dependency>
            <groupId>com.sk89q.worldguard</groupId>
            <artifactId>worldguard-bukkit</artifactId>
            <version>7.0.11</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <defaultGoal>clean package</defaultGoal>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.3</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <finalName>BlissGems-${project.version}</finalName>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>dev.xoperr.blissgems.BlissGems</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <!-- Shade FastStats into the JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <relocations>
                                <relocation>
                                    <pattern>dev.faststats</pattern>
                                    <shadedPattern>dev.xoperr.blissgems.libs.faststats</shadedPattern>
                                </relocation>
                            </relocations>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- ProGuard Obfuscation Plugin - Standalone -->
            <plugin>
                <groupId>com.github.wvengen</groupId>
                <artifactId>proguard-maven-plugin</artifactId>
                <version>2.6.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>proguard</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <obfuscate>true</obfuscate>
                    <injar>${project.build.finalName}.jar</injar>
                    <outjar>${project.build.finalName}-obfuscated.jar</outjar>
                    <libs>
                        <lib>${java.home}/jmods/java.base.jmod</lib>
                        <lib>${java.home}/jmods/java.desktop.jmod</lib>
                    </libs>
                    <options>
                        <!-- Don't shrink or optimize - only obfuscate -->
                        <option>-dontshrink</option>
                        <option>-dontoptimize</option>
                        <option>-dontwarn</option>
                        <option>-ignorewarnings</option>

                        <!-- Keep main plugin class and JavaPlugin extension -->
                        <option>-keep public class dev.xoperr.blissgems.BlissGems {
    public void onEnable();
    public void onDisable();
    public void onLoad();
    public protected *;
}</option>

                        <!-- Keep all Bukkit/Spigot required classes -->
                        <option>-keep public class * extends org.bukkit.plugin.java.JavaPlugin</option>
                        <option>-keep class * implements org.bukkit.event.Listener { *; }</option>
                        <option>-keep class * implements org.bukkit.command.CommandExecutor { *; }</option>
                        <option>-keep class * implements org.bukkit.command.TabCompleter { *; }</option>
                        <option>-keep class * implements org.bukkit.configuration.serialization.ConfigurationSerializable { *; }</option>

                        <!-- Keep enum methods that Bukkit/Java needs -->
                        <option>-keepclassmembers enum * {
    public static **[] values();
    public static ** valueOf(java.lang.String);
    **[] $VALUES;
    public *;
}</option>

                        <!-- Keep public API classes for other plugins -->
                        <option>-keep public class dev.xoperr.blissgems.BlissGems {
    public *;
}</option>
                        <option>-keep public class dev.xoperr.blissgems.utils.** {
    public *;
}</option>
                        <option>-keep public class dev.xoperr.blissgems.managers.** {
    public *;
}</option>
                        <!-- Keep Addon API classes (used by external plugins) -->
                        <option>-keep public class dev.xoperr.blissgems.api.** { *; }</option>
                        <option>-keep public interface dev.xoperr.blissgems.api.** { *; }</option>
                        <option>-keep public enum dev.xoperr.blissgems.utils.GemType {
    *;
}</option>

                        <!-- Keep relocated FastStats classes and Gson (uses reflection) -->
                        <option>-keep class dev.xoperr.blissgems.libs.faststats.** { *; }</option>
                        <option>-keep class com.google.gson.** { *; }</option>

                        <!-- Preserve annotations for Bukkit -->
                        <option>-keepattributes *Annotation*</option>

                        <!-- Keep line numbers for stack traces but remove variable names -->
                        <option>-keepattributes SourceFile,LineNumberTable</option>
                        <option>-keepattributes !LocalVariableTable,!LocalVariableTypeTable</option>

                        <!-- Obfuscation settings (safe for Paper) -->
                        <option>-allowaccessmodification</option>

                        <!-- Print mapping file for debugging (if needed) -->
                        <option>-printmapping ${project.build.directory}/proguard-mapping.txt</option>

                        <!-- Keep WorldGuard API classes (soft dependency) -->
                        <option>-dontwarn com.sk89q.worldguard.**</option>
                        <option>-dontwarn com.sk89q.worldedit.**</option>
                    </options>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
